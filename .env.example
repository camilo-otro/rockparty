# Copy this file to .env and fill in real values from your Supabase project
# Settings > API. Must be prefixed PUBLIC_ so SvelteKit exposes them
# client-side (see src/lib/supabaseClient.js).

PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
